# Entity: health

main: kit: entity: health: {

  alias: field: {}

  fields: [
    {
      name: "name"
      req: false
      short: "Service name"
      type: "`$STRING`"
    }
    {
      name: "status"
      req: false
      short: "Service status"
      type: "`$STRING`"
    }
  ]

  name: "health"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/health"
          parts: [
            "health"
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.services`"
          }
        }
      ]
    }
  }



}
